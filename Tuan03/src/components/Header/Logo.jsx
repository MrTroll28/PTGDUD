import React from "react";
import logo from "/logo.png";

const Logo = () => {
    return (
        <div className="logo">
            <img src={logo} alt="Cheffy Logo" />
        </div>
    );
};

export default Logo;
